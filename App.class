// Source code is decompiled from a .class file using FernFlower decompiler (from Intellij IDEA).
import java.util.Scanner;

public class App {
   private static final int MAX_EVENTS = 100;
   private final Calendar[] calendars = new Calendar[100];
   private final Scanner scanner;

   public App() {
      this.scanner = new Scanner(System.in);
   }

   public void run() {
      boolean var1 = true;

      while(var1) {
         System.out.println("Add your event here or type 'exit' to quit:");
         String var2 = this.scanner.nextLine();
         if (var2.equalsIgnoreCase("exit")) {
            var1 = false;
            System.out.println("Goodbye!");
         } else {
            System.out.println("Enter event name:");
            String var3 = this.scanner.nextLine();
            System.out.println("Enter event date (YYYY-MM-DD):");
            String var4 = this.scanner.nextLine();
            System.out.println("Enter event time (HH:MM AM/PM):");
            String var5 = this.scanner.nextLine();
            System.out.println("Enter event location:");
            String var6 = this.scanner.nextLine();
            System.out.println("Enter event season:");
            String var7 = this.scanner.nextLine();
            System.out.println("Enter event notes:");
            String var8 = this.scanner.nextLine();
            if (!DateValidator.isValidDate(var4)) {
               System.out.println("Invalid date format. Please use YYYY-MM-DD.");
            } else {
               Calendar var9 = new Calendar(var3, var4, var5, var6, var7);
               var9.setNotes(var8);

               for(int var10 = 0; var10 < this.calendars.length; ++var10) {
                  if (this.calendars[var10] == null) {
                     this.calendars[var10] = var9;
                     break;
                  }
               }

               System.out.println("Event saved: " + String.valueOf(var9));
            }
         }
      }

      this.scanner.close();
   }

   public static void main(String[] var0) {
      (new App()).run();
   }
}
